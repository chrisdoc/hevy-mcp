codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        target: 85%
        threshold: 0%
        base: auto
    patch:
      default:
        target: 90%
        threshold: 0%
        base: auto

ignore:
  - "src/generated/**"

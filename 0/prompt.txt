## Your Assigned Feature

```json
{
  "id": "ops-training-summary",
  "description": "Implement workflows.trainingSummary as an operations composite: Stream.paginate, DateTime UTC windows, dual scan via Effect.all with bounded concurrency, TestClock-deterministic tests, matching current aggregation/scan evidence contracts.",
  "skillName": "operations-worker",
  "milestone": "ops-complete",
  "preconditions": [
    "workouts.list and bodyMeasurements.list Effect operations exist",
    "Current get-training-summary behavior is documented in packages/core/src/tools/workflows.ts tests"
  ],
  "expectedBehavior": [
    "trainingSummary is an operations composite using Stream.paginate, yield* DateTime.now (not DateTime.now() / new Date()), Effect.all bounded concurrency, TestClock.layer(), in-memory adapters only",
    "weeks 1–12; last weeks*7 UTC days; pageSize 10; in-window keep vs scanned-only; measurement sort/earliest/latest/weight_change_kg; workout aggregation matches current contract; scan evidence returned; unparseable window → empty loader; stop on empty/pageCount; later-page 404 ends a loader; page 1 404 fails"
  ]
}
```
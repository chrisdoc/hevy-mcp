create another PR to remove prettier, we should only use oxfmt     await runCommand(
            prettier.command,
            [...prettier.args, "--ignore-unknown", "--write", generatedRelative],
            fixture.client,
        );
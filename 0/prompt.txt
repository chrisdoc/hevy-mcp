## Your Assigned Feature

```json
{
  "id": "open-github-pr",
  "description": "Open a GitHub pull request against main from feat/effect-ops-layer using gh. Never push to main. Leave /root/hevy-mcp original checkout untouched.",
  "skillName": "cli-worker",
  "milestone": "cli-hygiene",
  "preconditions": [
    "Feature branch feat/effect-ops-layer contains all commits and the cascade Changeset",
    "gh is authenticated as chrisdoc with repo scope",
    "release-hygiene-and-changeset completed"
  ],
  "expectedBehavior": [
    "gh pr create targets main from feat/effect-ops-layer (not from main)",
    "PR body summarizes Effect-first operations, cache, workflows, CLI, and validation",
    "main is not force-pushed or directly pushed",
    "Original checkout /root/hevy-mcp remains on its prior branch with only the pre-existing staged amber-rivers-heal changeset"
  ]
}
```
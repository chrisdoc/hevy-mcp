runtime: "typescript"
# Optional fields like name/description/links are inferred automatically for the
# TypeScript runtime. Configuration is handled via the exported configSchema in
# src/index.ts, which prompts for HEVY_API_KEY.

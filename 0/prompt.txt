## Your Assigned Feature

```json
{
  "id": "client-worktree-and-effect-surface",
  "description": "Create the mission worktree from origin/main (feat/effect-ops-layer at ../hevy-mcp-effect-ops-layer). Expand HevyRequestEffectClient to all 22 public HevyClient methods as first-class Effects sharing the native request interpreter. Keep public HevyClient Promise-shaped. Preserve hidden NATIVE_REQUEST_EFFECT, internal export path, HTTP matrix parity, encoding, Promise createRoutine empty-body quirk, and independent Effect vs Promise fetches.",
  "skillName": "effect-client-worker",
  "milestone": "client-seam",
  "preconditions": [
    "Original checkout /root/hevy-mcp is not used for implementation",
    "Worktree ../hevy-mcp-effect-ops-layer does not yet exist or is this mission's branch",
    "effect@4.0.0-rc.112 is a hevy-client dependency",
    "Public HevyClient already has 22 Promise methods; Effect client currently has only 4 reads"
  ],
  "expectedBehavior": [
    "Feature work happens on branch feat/effect-ops-layer in /root/hevy-mcp-effect-ops-layer based on origin/main",
    "HevyRequestEffectClient exposes Effect methods for every public HevyClient method",
    "Public HevyClient methods still return Promise and HevyClient types stay Promise<...>",
    "Main/types/schemas exports do not leak Effect client symbols; ./internal still exports the seam",
    "NATIVE_REQUEST_EFFECT remains non-enumerable; getNativeRequestEffect recovers it or throws TypeError",
    "Effect vs Promise HTTP method/URL/path/query/body/200-201 payload match for all 22 methods",
    "Promise createRoutine still maps {} to undefined; Effect createRoutine returns native payload and does not call the Promise method",
    "Invoking Effect and Promise twins issues two interpreter fetches",
    "Encoding parity for page/pageSize/since/start_date/end_date and path keys workoutId/routineId/exerciseTemplateId/folderId/date",
    "Generated client is not hand-edited in this feature"
  ]
}
```
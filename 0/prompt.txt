## Your Assigned Feature

```json
{
  "id": "user-testing-validator-core-runtime",
  "description": "User testing validation for milestone \"core-runtime\". Determines testable assertions from features' fulfills field, sets up environment, spawns flow validator subagents, synthesizes results. Always returns to orchestrator.",
  "skillName": "user-testing-validator",
  "milestone": "core-runtime",
  "preconditions": [
    "All implementation features for milestone \"core-runtime\" are complete"
  ],
  "expectedBehavior": [
    "Testable assertions determined from fulfills mapping",
    "Environment set up (services started, data seeded)",
    "Flow validator subagents spawned and completed",
    "Results synthesized, validation-state.json updated"
  ]
}
```